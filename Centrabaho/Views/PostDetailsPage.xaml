<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Centrabaho.Views.PostDetailsPage"
             Title="PostDetailsPage">
    <!--<Grid ColumnDefinitions="100,*">
        <Frame Grid.Column="0" CornerRadius="0" BackgroundColor="#ECBA82">
            <Grid RowDefinitions="Auto,Auto,Auto,Auto,*,Auto" HorizontalOptions="FillAndExpand">
                <Button Grid.Row="3"
                    Clicked="createpostBtn"
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"/>
                <Button Grid.Row="2"
                    
                    HorizontalOptions="FillAndExpand"
                    VerticalOptions="FillAndExpand"/>
                <Button Grid.Row="5"
                    Command="{Binding SignOutCommand}"/>
            </Grid>
        </Frame>
        <ScrollView Grid.Column="1">
            <Grid  RowDefinitions="Auto,Auto,*">


            </Grid>

        </ScrollView>
    </Grid>-->
</ContentPage>